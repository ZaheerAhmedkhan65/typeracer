<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-primary">
    <header class="d-flex justify-content-between p-1">
        <% if (user) { %>
        <a class="fw-bold text-decoration-none white btn btn-secondary" href="/race">Start Game</a>
        <% } %>
        <% if (!user) { %>
            <a class="fw-bold text-decoration-none white btn btn-secondary" href="/race" disable>Start Game</a>
        <% } %>

       
        <div class="d-flex justify-content-center align-items-center gap-1">
            <% if (user) { %>
            <a class="fw-bold text-decoration-none white btn btn-info-outline" href="/logout" onclick="return confirm('Are you sure to Log Out?')">Log Out</a>
            <a class="fw-bold text-decoration-none white btn btn-info-outline" href="/user/profile">Profile</a>
            <% } %>
            <% if (!user) { %>
                <a class="fw-bold text-decoration-none white btn btn-info" href="/users/login">Log in</a>
                <a class="fw-bold text-decoration-none white btn btn-info-outline" href="/register/new">Sign Up</a>
            <% } %>
        </div>
    </header>
    <div class="container d-flex flex-column vh-85">